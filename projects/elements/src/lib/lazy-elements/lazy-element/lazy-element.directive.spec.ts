import { LazyElementDirective } from './lazy-element.directive';

describe('LazyElementDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyElementDirective();
    expect(directive).toBeTruthy();
  });
});
